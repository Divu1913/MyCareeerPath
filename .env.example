# Frontend environment example.
# Copy this to .env (or .env.local) and adjust as needed.
# Vite reads vars prefixed with VITE_.

# Base URL of the FastAPI backend.
VITE_API_BASE="http://127.0.0.1:8000"
