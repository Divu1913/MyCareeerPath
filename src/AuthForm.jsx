import { useEffect, useRef, useState } from "react";
import { api, auth, formatAccountError, isAccountRoleConflict } from "./api.js";
import ForgotPasswordModal from "./components/modals/ForgotPasswordModal.jsx";

const ROLES = [
  { key: "candidate", label: "Candidate", accent: "#2563eb", subtitle: "Roles matched to your skills, tracked in one place." },
  { key: "recruiter", label: "Recruiter", accent: "#d97706", subtitle: "Manage pipelines, shortlists, and interviews." },
];

function normalizeIndianPhone(raw) {
  let digits = (raw || "").replace(/\D/g, "");
  if (digits.length === 13 && digits.startsWith("910")) digits = digits.slice(3);
  else if (digits.length === 12 && digits.startsWith("91")) digits = digits.slice(2);
  else if (digits.length === 11 && digits.startsWith("0")) digits = digits.slice(1);
  return /^[6-9]\d{9}$/.test(digits) ? digits : null;
}

export default function AuthForm({ onSuccess, onHome, initialMode = "signin", initialRole = "candidate" }) {
  const [activeTab, setActiveTab] = useState(initialRole === "recruiter" ? "recruiter" : "candidate");
  const [mode, setMode] = useState(initialMode === "signup" ? "signup" : "login");
  const [step, setStep] = useState("identifier");
  const [loginMethod, setLoginMethod] = useState("email");
  const [email, setEmail] = useState(""); const [phone, setPhone] = useState(""); const [identifier, setIdentifier] = useState("");
  const [fullName, setFullName] = useState(""); const [password, setPassword] = useState(""); const [code, setCode] = useState(["", "", "", "", "", ""]);
  const [devCode, setDevCode] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const [showForgot, setShowForgot] = useState(false);
  const inputsRef = useRef([]); const currentRole = ROLES.find((role) => role.key === activeTab);
  const inputClass = "block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm outline-none transition focus:ring-2 focus:ring-blue-100";

  useEffect(() => setIdentifier(loginMethod === "email" ? email.trim() : (normalizeIndianPhone(phone) || phone.trim())), [loginMethod, email, phone]);
  useEffect(() => { if (mode === "signup" && step === "verify" && code.every(Boolean) && !loading) verifySignup(); }, [code]); // eslint-disable-line react-hooks/exhaustive-deps

  function getIdentifier() {
    if (loginMethod === "email") { if (!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email.trim())) throw new Error("Please enter a valid email address."); return email.trim(); }
    const normalized = normalizeIndianPhone(phone); if (!normalized) throw new Error("Please enter a valid 10-digit Indian mobile number."); return normalized;
  }
  function switchMode(nextMode) { setMode(nextMode); setStep("identifier"); setCode(["", "", "", "", "", ""]); setPassword(""); setError(""); }
  async function submitIdentifier(event) {
    event.preventDefault(); setError(""); let value; try { value = getIdentifier(); } catch (err) { setError(err.message); return; }
    if (mode === "login") {
      if (!password) { setError("Password is required."); return; }
      setLoading(true); try { const result = await api.login({ identifier: value, password }); auth.setTokens(result); onSuccess?.(); } catch (err) { setError(formatAccountError(err, "Unable to sign in")); } finally { setLoading(false); } return;
    }
    if (!fullName.trim()) { setError("Full name is required."); return; }
    setLoading(true); try { const result = await api.sendOtp({ identifier: value, full_name: fullName.trim(), role: activeTab, is_signup: true }); setIdentifier(result.identifier); setDevCode(result.dev_code || ""); setStep("verify"); } catch (err) { if (isAccountRoleConflict(err)) switchMode("login"); setError(formatAccountError(err, "Unable to send OTP")); } finally { setLoading(false); }
  }
  function updateCode(index, value) { const digits = value.replace(/\D/g, ""); const next = [...code]; if (digits.length > 1) digits.slice(0, 6).split("").forEach((digit, offset) => { if (index + offset < 6) next[index + offset] = digit; }); else next[index] = digits; setCode(next); if (digits && index < 5) inputsRef.current[Math.min(index + digits.length, 5)]?.focus(); }
  async function verifySignup(event) { event?.preventDefault(); const joined = code.join(""); if (joined.length !== 6) { setError("Enter all 6 digits."); return; } setLoading(true); setError(""); try { await api.verifyOtp({ identifier, code: joined, role: activeTab, is_signup: true }); setStep("password"); } catch (err) { setError(formatAccountError(err, "Verification failed")); } finally { setLoading(false); } }
  async function createAccount(event) { event.preventDefault(); if (password.length < 8) { setError("Password must be at least 8 characters."); return; } setLoading(true); setError(""); try { const result = await api.register({ identifier, password, role: activeTab, full_name: fullName.trim() }); auth.setTokens(result); onSuccess?.(); } catch (err) { setError(formatAccountError(err, "Unable to create account")); } finally { setLoading(false); } }

  return <div className="min-h-screen flex flex-col bg-[var(--theme-cream)]"><header className="w-full border-b border-slate-200 bg-[#fffdf5]/95"><div className="mx-auto flex h-16 max-w-3xl items-center justify-center px-4"><button type="button" onClick={onHome} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-700">← BACK to Home</button></div></header><main className="flex flex-1 items-center justify-center px-4 py-8"><div className="w-full max-w-md overflow-hidden rounded-2xl border border-[var(--theme-border)] bg-white shadow-lg">{step === "identifier" && <div className="flex border-b border-slate-200">{ROLES.map((role) => <button key={role.key} type="button" onClick={() => { setActiveTab(role.key); setError(""); }} className={`flex-1 border-b-2 py-3 text-sm font-semibold ${activeTab === role.key ? "text-slate-900" : "border-transparent text-slate-500"}`} style={activeTab === role.key ? { borderColor: role.accent, color: role.accent } : {}}>{role.label}</button>)}</div>}<div className="p-8">{step === "identifier" ? <form onSubmit={submitIdentifier} className="space-y-5"><div className="flex rounded-xl bg-slate-100 p-1"><button type="button" onClick={() => switchMode("login")} className={`flex-1 rounded-lg py-1.5 text-xs font-semibold ${mode === "login" ? "bg-white shadow" : "text-slate-500"}`}>Sign In</button><button type="button" onClick={() => switchMode("signup")} className={`flex-1 rounded-lg py-1.5 text-xs font-semibold ${mode === "signup" ? "bg-white shadow" : "text-slate-500"}`}>Sign Up (Register)</button></div><div><h1 className="text-xl font-bold text-slate-800">{mode === "login" ? `${currentRole.label} Sign In` : `Register as ${currentRole.label}`}</h1><p className="mt-1 text-xs text-slate-500">{currentRole.subtitle}</p></div><div className="space-y-4"><div><span className="block text-xs font-semibold uppercase tracking-wider text-slate-600">Login with:</span><div className="mt-2 flex gap-6"><label className="text-sm"><input type="radio" checked={loginMethod === "email"} onChange={() => setLoginMethod("email")} /> Email</label><label className="text-sm"><input type="radio" checked={loginMethod === "mobile"} onChange={() => setLoginMethod("mobile")} /> Mobile</label></div></div>{loginMethod === "email" ? <label className="block text-xs font-semibold uppercase tracking-wider text-slate-600">Email Address<input type="email" required value={email} onChange={(event) => setEmail(event.target.value)} autoComplete="username" placeholder="you@example.com" className={`${inputClass} mt-1 font-normal`} /></label> : <label className="block text-xs font-semibold uppercase tracking-wider text-slate-600">Mobile Number<div className="mt-1 flex"><span className="rounded-l-lg border border-r-0 border-slate-300 bg-slate-50 px-3 py-2 text-sm text-slate-600">+91</span><input type="tel" required inputMode="numeric" maxLength={14} value={phone} onChange={(event) => setPhone(event.target.value.replace(/\D/g, "").slice(0, 14))} autoComplete="tel-national" placeholder="9876543210" className="block w-full rounded-r-lg border border-slate-300 px-3 py-2 text-sm font-normal outline-none" /></div></label>}{mode === "login" ? <><label className="block text-xs font-semibold uppercase tracking-wider text-slate-600">Password<input type="password" required value={password} onChange={(event) => setPassword(event.target.value)} autoComplete="current-password" className={`${inputClass} mt-1 font-normal`} /></label><button type="button" onClick={() => setShowForgot(true)} className="text-sm font-semibold hover:underline" style={{ color: currentRole.accent }}>Forgot Password?</button></> : <label className="block text-xs font-semibold uppercase tracking-wider text-slate-600">Full Name<input type="text" required value={fullName} onChange={(event) => setFullName(event.target.value)} autoComplete="name" className={`${inputClass} mt-1 font-normal`} /></label>}</div>{error && <p className="rounded-lg border border-red-100 bg-red-50 p-3 text-xs font-medium text-red-600" role="alert">{error}</p>}<button disabled={loading} type="submit" className="w-full rounded-xl py-3 font-bold text-white disabled:opacity-60" style={{ backgroundColor: currentRole.accent }}>{loading ? (mode === "login" ? "Signing in…" : "Sending code…") : (mode === "login" ? "Sign In" : "Send OTP")}</button></form> : step === "verify" ? <form onSubmit={verifySignup} className="space-y-6"><div><h1 className="text-xl font-bold text-slate-800">Verify your email or mobile</h1><p className="mt-1 text-xs text-slate-500">Enter the code sent to <b>{identifier}</b>.</p></div>{devCode && <p className="rounded-lg border border-amber-200 bg-amber-50 p-3 text-xs text-amber-900">Development OTP: <b>{devCode}</b></p>}<div className="flex justify-between gap-2">{code.map((digit, index) => <input key={index} ref={(element) => { inputsRef.current[index] = element; }} value={digit} onChange={(event) => updateCode(index, event.target.value)} onKeyDown={(event) => { if (event.key === "Backspace" && !code[index] && index) inputsRef.current[index - 1]?.focus(); }} inputMode="numeric" maxLength={6} className="h-14 w-12 rounded-lg border border-slate-300 text-center text-2xl font-bold" />)}</div>{error && <p className="rounded-lg bg-red-50 p-3 text-xs text-red-600">{error}</p>}<button disabled={loading} className="w-full rounded-xl py-3 font-bold text-white disabled:opacity-60" style={{ backgroundColor: currentRole.accent }}>{loading ? "Verifying…" : "Verify OTP"}</button></form> : <form onSubmit={createAccount} className="space-y-5"><div><h1 className="text-xl font-bold text-slate-800">Create your password</h1><p className="mt-1 text-xs text-slate-500">Your email/mobile is verified. Set a password to finish registration.</p></div><label className="block text-xs font-semibold uppercase tracking-wider text-slate-600">Password<input type="password" required minLength={8} value={password} onChange={(event) => setPassword(event.target.value)} autoComplete="new-password" className={`${inputClass} mt-1 font-normal`} /></label>{error && <p className="rounded-lg bg-red-50 p-3 text-xs text-red-600">{error}</p>}<button disabled={loading} className="w-full rounded-xl py-3 font-bold text-white disabled:opacity-60" style={{ backgroundColor: currentRole.accent }}>{loading ? "Creating account…" : "Create Account"}</button></form>}</div></div></main><ForgotPasswordModal isOpen={showForgot} onClose={() => setShowForgot(false)} initialIdentifier={identifier} onComplete={() => { setMode("login"); setStep("identifier"); setPassword(""); setError("Password reset successfully. Please sign in."); }} /></div>;
}
